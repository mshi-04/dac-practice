# Copy to backend.hcl, replace the values, and keep the copy untracked.
bucket       = "replace-with-terraform-state-bucket"
key          = "dac-practice/verification/terraform.tfstate"
region       = "replace-with-aws-region"
encrypt      = true
use_lockfile = true
